[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -v --tb=short
markers =
    unit: pure unit tests, no external I/O
    integration: full request/response cycle via TestClient
    auth: authentication and authorization tests
    edge: boundary conditions and error paths
